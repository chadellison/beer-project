import React, { Component } from "react"
import "../App.css"

export default class Contact extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <a href="#">contact</a>
    )
  }
}
