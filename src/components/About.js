import React, { Component } from "react"
import "../App.css"

export default class About extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <a href="#">about</a>
    )
  }
}
